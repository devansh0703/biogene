kind = "web"
previewPath = "/"
title = "BioGene"
version = "1.0.0"
id = "artifacts/biogene"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20899

[services.development]
run = "pnpm --filter @workspace/biogene run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/biogene", "run", "build" ]
publicDir = "artifacts/biogene/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20899"
BASE_PATH = "/"
