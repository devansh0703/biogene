kind = "web"
previewPath = "/"
title = "BioForge"
version = "1.0.0"
id = "artifacts/bioforge"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20899

[services.development]
run = "pnpm --filter @workspace/bioforge run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/bioforge", "run", "build" ]
publicDir = "artifacts/bioforge/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20899"
BASE_PATH = "/"
